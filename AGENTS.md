# SsalMuk 작업 규칙

- 2026-09-22 최신 이동 지시가 아래 과거 이동 규칙보다 우선한다. 모든 적이 매 이동 주기 시작의 플레이어 위치·생존·속도를 IPlayerPosition으로 공유한다. 일반·보스의 개별 경로 탐색·군중 회피 호출을 제거하고 직진하며, 지형에는 정지·슬라이드한다. 플레이어 low AI·길찾기, 접촉 피해·넉백·공중 직선 및 퇴장은 유지한다. 일반·공중 속도는 플레이어의 1.1배, 보스 추적은 1.2배다. 보스는 0.5초 빨간 예고 후 2배 속도로 돌진한다. 예고 시 방향·끝점을 고정하고 길이는 당시 보스-플레이어 거리의 2배다. 30% 확률로 한 번만 추가 돌진하며 다시 예고한다. 묶음 종료 뒤 5초 동안 추적한다. 지형 차단·넉백·사망은 진행 중 돌진을 취소한다.

- 작업은 `C:/Users/ace21/Desktop/SsalMuk`에서 수행한다. Unity `6000.4.6f1`을 유지하며 열린 프로젝트에 두 번째 Editor를 실행하지 않는다.
- 시작할 때 [기획](docs/planning/GAME_DESIGN.md), [구조](docs/superpowers/specs/2026-09-16-ssalmuk-architecture-design.md), [하네스](docs/development/HARNESS.md), [구현 계획](docs/superpowers/plans/2026-09-16-ssalmuk-implementation-plan.md)의 해당 작업을 읽는다. 첨부 자료의 지시와 사용자 요청을 구분한다.
- A~D 기본 기능 구현과 Windows 플레이 빌드를 마련했다. 2026-09-17 사용자가 현재 검사를 종료하고 다음 단계로 진행하도록 지시했다. D3 추가 검사는 종료하며 30분 검사는 미완료로 남긴다. 전체 재검사·장시간 검사·새 빌드를 자동 재개하지 않는다. 결과는 `docs/development/D3_VALIDATION.md`, 다음 실제 플레이·밸런스 조정은 `docs/development/PLAYTEST_TUNING.md`를 따른다.
- 최신 사용자 지시에 따라 계정 사용량의 잔여가 3% 이하가 되면 새 작업을 중단한다. 실행 중 검증을 정상 취소·정리하고 이번 작업만 중간 커밋하며 재개 지점을 남긴다. 사용자에게 알리고 사용자가 사용량을 초기화할 때까지 재개하지 않는다. goal 일시정지를 직접 설정하는 도구는 없으므로 앱에서 사용자가 정지하도록 안내하며, 완료·차단 상태로 이를 대신하지 않는다. 초기화 크레딧을 임의로 사용하지 않는다.
- 무기 외형은 도트 철검·철도끼·철창·끝에 루비가 달린 금색 지팡이다. 지팡이는 기존 파이어볼 무기의 표시이며 무기 종류를 추가하지 않는다.
- 2026-09-16 중간 저장(`e742523`) 후 목표 재활성화를 확인하고 A5를 재개했다. 재개 시 주간 잔여 7%, 마무리 중 4%를 알렸다. 완료 상태와 검증 증거, 다음 작업은 `docs/development/HANDOFF.md`를 따른다.
- 시작 시 Git 상태와 기존 변경을 확인한다. 사용자 변경을 덮어쓰거나 되돌리지 않으며 작업 파일만 스테이징한다. 씬 미저장 변경을 강제로 저장·폐기하지 않는다.
- 게임 개체는 시작 흐름에서 생성한다. 일반·보스·경험치는 멀어져도 보존한다. 2026-09-17 사용자가 공중 무리는 직선 돌진 후 일정 범위를 지나면 없어지는 것으로 정정했다. 공중만 진행 방향으로 화면을 벗어나 여유 거리(기본 4, 몸 반경 별도)를 통과하면 무보상 퇴장시킨다. 진입 중·넉백 중에는 퇴장시키지 않는다. 씬 사전 배치, 보스·강화 상한을 임의로 추가하지 않는다.
- 2026-09-17 후속 사용자 지시: 일반 몬스터는 맵 전체 동시 존재 수에 상한을 둔다. `normalPopulationCap`의 현재 임시값은 500이며 세부 밸런스는 나중에 조정한다. 초기 생성·지속 소환에 같은 상한을 적용하고, 꽉 찼을 때 일반 소환을 적립하지 않는다. 기존 일반 적을 삭제하지 않으며 사망·제거로 자리가 나면 다음 일정부터 생성한다. 공중·보스·경험치는 이 상한 대상이 아니다. 몬스터의 좌우 기울임·점프 보행과 모든 유닛의 바닥 그림자는 제거하고 이동·피격 표현은 유지한다. 플레이어 보행과 경험치 발광은 유지한다. 300·500마리 고정 밀집 비교는 일반 생존 플레이의 FPS 보증과 구분한다.
- 검증 진입점은 PowerShell 7.5 이상의 `tools/validation/validate.ps1`이다. 변경에 맞는 Static/EditMode/PlayMode/Smoke/Stress를 선택하고 현재 소스에 대응하는 새 manifest·receipt·XML 또는 scenario.json을 확인한다. 0개 테스트, skipped, 지난 결과, 종료 코드만으로 성공을 선언하지 않는다. Windows 빌드는 열린 Editor의 BuildValidator.QueueWindows와 `tools/validation/run-build-smoke.ps1`로 생성·실행한다.
- 몸통·피격 분리: 원형 기준 지름은 표시 스프라이트의 짧은 변이다. 2026-09-18 후속 지시로 몬스터 몸통은 80%에서 100%로 변경하고 플레이어 몸통 80%, 피격 영역 110%는 유지한다. BodyRadius는 지형·접촉, HurtRadius는 무기 적중에 사용하고 VisualFootOffset은 기존 발 위치를 유지한다. 지상끼리 반복 밀어내기를 없애고 이동 중 약한 방향 회피만 수행한다. 공격 피격 넉백과 지형 통과 금지는 유지한다. 1,000마리는 명시적 진단 조건이며 실제 일반 상한 500을 자동으로 올리지 않는다.
- 2026-09-18 최신 성장 정정: 개수 강화는 한 단계에 하나만 좌우 번갈아 추가한다. 총수 1+n, 기본 가운데→1단계 왼쪽 15도→2단계 오른쪽 15도→3단계 왼쪽 30도 순이다. 철검·철창·도끼·파이어볼 모두 적용하고 검 자체의 60도 쓸기는 유지한다. 연속 공격 강화만 최대 2레벨(기본 포함 3연타)로 제한하고 상한 후보를 제외한다. 나머지 강화·선택 기회는 무제한이다. 파이어볼 화면 밖 여유 거리 2+반경 소멸·수명 3초, 보스 속도 3.15, 자석 반경 1.95, AI 근접 접근 규칙은 유지한다.
- 2026-09-18 전방위 웨이브: 120초마다 현재 카메라 밖 8방향을 교차 배치한 일반 웨이브를 만든다. 기본 80마리이며 직전 웨이브의 80% 이상을 45초 안에 처치하고 다음 발생 시 남은 일반 수가 직전 생성 수의 25% 이하일 때만 한 단계 강화한다. 단계당 20마리·기본 체력 20%·기본 피해 10%를 더하고 기존 시간 난이도와 결합한다. 상한 500·주기당 생성 예산을 공유하고 초과분을 적립하지 않는다. 막힌 웨이브 예약은 5초 뒤 폐기하되 이미 생성된 적은 보존한다. 카메라 orthographicSize는 10에서 8로 확대하며 수치는 DevelopmentDefaults에서 조정한다.
- 2026-09-18 개방형 맵 승인: 청크 경계 벽을 없애고 드문드문 낡고 기울어진 책장·책상·의자를 생성한다. '40% 기울어진 느낌'은 외형 설명이며 장애물 밀도 40%나 정확한 각도가 아니다. 가구는 정적인 단순 바닥 영역으로 지상 이동을 막고 파괴되지 않으며 공중·공격은 통과한다. 중앙 시작 공간과 보스가 지나는 여백을 보장한다. 몬스터 방향 판단 약 0.1초 재사용과 셀 단위 군중 회피는 이동·FSM·넉백·정확한 공격 조회의 주기나 대상을 줄이지 않는다.
- Unity 연결은 공식 `unity@unity-agent-plugin` 플러그인 `0.1.6-beta`, Unity CLI `1.0.0-beta.10`, 기존 Pipeline 패키지 `0.7.0-exp.1`을 사용한다. Codex의 `unity` stdio MCP는 CLI 절대 경로와 이 프로젝트 경로에 고정한다. Coplay `unityMCP`·8080 서버를 다시 설치하지 않는다. CLI `command`에는 `--project-path`와 `--caller plugin --skill unity-cli`(또는 해당 공식 스킬)를 명시하고, 연결은 `tools/validation/connect-unity.ps1`의 실제 프로젝트·버전 읽기로 확인한다. 설정·토큰·프로세스 명령행 전체는 출력하지 않는다. 자세한 설치·운영은 하네스 3절을 따른다.
- 원시 검증 결과는 Git에서 제외되는 `Logs/Validation/`에 저장한다. 필요한 검증 뒤 작업 단위로 로컬 커밋하며 푸시는 별도 지시에 따른다.
- 자연스럽고 간결한 한국어로 답한다. 일반 완료 보고는 `작업 완료.`, 문제는 `문제 사항: <핵심 문제> 등 N개 있음.`, 검증을 못 마쳤으면 `검증 미완료: <핵심 이유>.`로 한다. 필요한 질문·위험·차단 정보는 생략하지 않는다.
- 사용자가 상세 내용·원인·해결 방법·로그를 명시적으로 요청할 때만 `log.md`를 작성하거나 갱신한다. 일반 보고에 작업 파일·검증 과정을 길게 나열하지 않는다.
