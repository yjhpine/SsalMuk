# 몸통·피격 분리와 1,000마리 검증 — 2026-09-17

**결론: 분리와 군중 처리 변경은 구현했지만, 1,000마리를 원활하게 처리한다고 판정할 수 없다.** Unity 밀집 조건에서 전투 포함 1,000마리의 모델 계산은 평균 70.74ms/고정 주기였다. 현재 20ms 고정 주기보다 길며 최대 17회 누적 갱신이 한 렌더 프레임에 몰렸다. 500 조건도 평균 41.32ms라 밀집 시 끊김이 남는다. 검증 통과는 개체·판정·측정 계약의 통과이고 목표 프레임률 통과가 아니다.

## 구현

- `UnitDefinition`에 BodyRadius, HurtRadius, VisualFootOffset을 분리했다. 실제 에셋을 Unity API로 변경해 원형 기준 지름(표시 스프라이트의 짧은 변)에 몸통 80%·피격 110%를 적용했다. 일반은 몸통 반경 약 .26, 피격 반경 약 .3575다. 원래 일반의 몸통도 이 기준에서는 이미 80% 수준이었으므로 반경 축소만으로 성능이 개선된 것으로 설명하지 않는다.
- 기존 발 위치를 별도 값으로 보존했다. 공격 피격 팽창·보행은 몸통이나 피격 반경을 바꾸지 않는다. 난이도 상승과 진단용 정의 복사도 독립 반경을 유지한다.
- 근접 무기·파이어볼 명중·폭발의 정확한 적중과 후보 검색 모두 HurtRadius를 사용한다. 지형·길찾기·플레이어 접촉 피해는 BodyRadius를 사용한다. 무기의 범위·궤적·피해·투사체 수명은 유지했다.
- 반복 겹침 분리와 접선 후보 평가를 제거했다. 이동 중인 지상 적만 근처 후보를 한 번 조회해 약하게 회피하며, 원래 이동량보다 빨라지거나 다른 개체 위치를 밀어내지 않는다. 정지 개체의 겹침은 그대로 두고 플레이어·공중에는 이 회피를 적용하지 않는다. 공격 피격 넉백은 회피와 분리해 기존 힘을 유지하고 최종 지형 쓸기 검사는 유지한다.
- 이동 유닛 목록과 회피 검색 결과 버퍼를 재사용한다. 검색 버퍼의 기존 원소 제거·ID 순서·청크 경계 결과를 검증했다. 아직 모든 공간 검색·지형 검사 할당을 없앤 것은 아니다.
- 게임의 일반 상한은 500을 유지했다. 1,000마리는 진단에서 직접 생성한 고정 개체 수이며 보스·공중·경험치 보존 규칙과 세부 능력치는 변경하지 않았다.

## Unity 계측 결과

Unity 6000.4.6f1 Editor, Ryzen 7 250, RTX 5060 Laptop, 1920×1080. 시드 260917, 간격 .58, 빈 지형, 카메라 반높이 24, 표시 반경 60. 실제 개발 에셋의 유닛 반경을 사용했다. 조건마다 예열 40주기 후 모델 350주기(게임 시간 7초)를 측정하고 렌더 간격 최소 20개를 요구했다. 고정 주기 .02초·최대 누적 .3333초는 바꾸지 않았다.

| 조건 | 모델 평균 / p95 ms | 표시 평균 ms | 프레임 간격 평균 ms | 렌더 표본 | 측정 벽시계 초 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 300, 이동만 | 23.51 / 41.91 | 2.45 | 77.85 | 113 | 9.17 |
| 500, 이동만 | 41.32 / 82.44 | 4.62 | 711.69 | 20 | 14.87 |
| 1,000, 이동만 | 70.78 / 153.16 | 9.27 | 1,218.44 | 20 | 25.37 |
| 1,000, 전투 포함 | 70.74 / 154.70 | 10.68 | 1,210.83 | 20 | 25.42 |

전투 포함 조건은 플레이어 AI, 기본 무기 4종, 접촉 피해, 초기 경험치 200개를 켰다. 플레이어와 몬스터 체력을 높여 1,000마리가 죽어 표본이 줄지 않게 했고 지속 소환은 껐다. 검/창/도끼/파이어볼 발동은 8/7/5/6회, 활성 투사체 최대 1개를 관측했다. 각 조건의 표시 유닛 최솟값은 몬스터 수+플레이어 1이고 그림자는 0이다.

모델 ms는 이동 또는 전투 시뮬레이션 한 번, 표시 ms는 Presenter/View 갱신 한 번, 프레임 간격은 실제 연속 LateUpdate 사이 시간이다. 표시 ms를 GPU 시간이나 전체 렌더 비용으로 해석하지 않는다. 할당 API는 이 Editor에서 지원 탐침을 통과하지 못해 -1을 기록했으며 0 할당을 뜻하지 않는다.

이전 그림자 제거 뒤 참고값은 300/500 모델 124.18/229.06ms다. 새 결과의 계산 비용은 크게 감소했으나 예열 기준, 반경 구성, 카메라·표시 범위가 바뀌어 엄밀한 동일 조건 개선율로 보고하지 않는다. 이번 300/500/1000끼리는 같은 새 조건을 적용했다. 한 번씩 수행한 짧은 밀집 비교이며 일반 생존 FPS, 장애물이 많은 장면, 높은 무기 강화, 장시간 경험치·방문 지형 누적, 빌드 성능은 보증하지 않는다.

## 검증과 저장

- 변경 전 정지 겹침 회귀 1개가 `ee91383ef40248c490d13269c452132c`에서 실패해 강제 위치 보정을 재현했다. 변경 후 새 정지·속도·넉백·벽·공중 회귀가 통과했다.
- 현재 소스 해시: `a6d5c8307a91ad962f03eb64ab59cec31d9760732e7392ebbdc6a0c03a854fc9`.
- 관련 EditMode 49개, 표시·피격 PlayMode 6개, 개체 수 비교 4개, 실제 GameStart·맵·초기 12마리·AI 경험치 수집·HUD 검사 1개로 총 60개가 통과했다. 0개 테스트나 과거 소스 결과를 성공 근거로 사용하지 않았다.
- 비교 실행: `57921a450ae5416696c358b91db8e77b`. 보고서 300=`c52c23dd192e48ddba24ed4d5acf35fb`, 500=`f3e636be4f3847d19000156718b871b7`, 1000 이동=`456e97668ebe41abbc2085cf460b782e`, 1000 전투=`9c7fc6d4128b4423a662e1b904973eb0`.
- 실제 시작 실행: `21c8726c417a4b8a87f74ac373d9ce1e`. 개별 기능 검사 실행 ID·receipt 대조·측정 JSON·에셋 반경 읽기와 사용자 설정 해시 보존은 `Logs/Validation/body-hit-separation-20260917/`에 기록했다.
- 새 장시간 검사·전체 검사·Windows 빌드는 실행하지 않았다. 작업 중 생성된 기존 커밋 `348e9e7`에 주요 구현이 포함되어 있음을 확인했다. 해당 커밋과 사용자 파일을 변경하지 않고 남은 검증 코드·문서만 후속 로컬 커밋으로 저장한다.

## 남은 비용과 다음 후보

큰 비용이 아직 이동 모델에 있다는 것은 위 비교로 확인했다. 개별 남은 함수의 Unity CPU 점유율은 이번에 따로 계측하지 않았다. 소스상 후보는 매 유닛의 지형 원형·쓸기 검사와 후보 셀 HashSet 생성, 가까운 적이 몰릴 때 증가하는 회피 후보 순회·정렬, 유닛별 길찾기 조회다. 회피가 한 번의 조회여도 같은 지점에 아주 많은 적이 몰리면 전체 후보 순회는 다시 제곱 규모가 될 수 있다.

다음 최적화는 이 구간을 Unity에서 나눠 계측한 뒤 지형 검사 중복·할당 제거와 밀집 회피 후보 집계/예산을 검토한다. 게임 상한을 1,000으로 올리거나 논리 유닛·피해를 생략해서 이번 성능 문제를 숨기지 않는다. 이 후보 개선은 아직 구현하지 않았다.

---

# 그림자 제거·일반 상한 500·Unity 300/500 비교 — 2026-09-17

사용자 승인에 따라 모든 유닛의 바닥 그림자를 제거하고 일반 몬스터 상한을 300에서 500으로 변경했다. 공식 MCP 연결을 확인한 열린 SsalMuk Editor에서 변경 전후를 각각 측정했다. 최종 시점에 MainMenu 씬, 재생 중지, 씬 미저장 변경 없음, 실제 GameCatalog의 상한 500과 그림자 프리팹 제거를 읽어 확인했다.

## 변경과 보존 범위

- `UnitView`의 런타임 GroundShadow 생성·갱신, UnitView 프리팹의 이전 Shadow 자식, 개발용 프리팹 생성기의 그림자 생성을 함께 제거했다.
- `SpawnSettings` 기본값, `DevelopmentDefaults` 기본값, 실제 연결된 DevelopmentDefaults 에셋을 모두 500으로 맞췄다. 초기·지속 일반 생성과 화면 밖 일반까지 합산하는 기존 상한 동작을 유지한다.
- 보스 300초 일정, 공중 퇴장, 경험치 보존·발광, 무기·공격 범위, 플레이어 보행과 피격 연출은 변경하지 않았다. 이번에는 군중 계산과 파이어볼 규칙도 변경하지 않았다.
- 기획·구조·하네스·구현 계획·플레이 조정 문서·AGENTS를 최신 결정에 맞췄다. 기존 사용자 ProjectSettings 두 파일의 SHA256이 시작 전과 동일하며 관련 없는 변경은 스테이징하지 않는다.

## 실제 Unity 측정

Unity 6000.4.6f1 Editor PlayMode, Ryzen 7 250, RTX 5060 Laptop, 1920×1080. 시드 260917, 간격 0.58의 행 우선 배치, 빈 지형, 플레이어 이동 입력 없음(군중 위치 보정은 유지), 일반 300 또는 500을 실제 WorldPresenter/UnitView로 모두 표시했다. 공격·처치·플레이어 AI·소환·경험치는 끈 밀집 이동 진단이다. 실제 FixedUpdate 0.02초와 기존 최대 누적 시간 0.3333초를 유지했다. VSync 0, 목표 FPS 제한 없음, 백그라운드 실행 상태로 동일 비교한 뒤 이 세 설정을 원복했다. 컴파일 최적화 Release, Profiler·Deep Profile 꺼짐, 디버거 미연결 상태를 측정 전에 확인했다.

2초 예열 뒤 각 경우 20개의 연속 LateUpdate 간격과 350회 이동 갱신(게임 시간 7초)을 수집했다. 측정 벽시계 시간은 300마리 전/후 43.96/44.19초, 500마리 전/후 77.20/80.81초다. 각 조건 1회 측정이며 통계적 성능 보증이 아니다. 예열은 벽시계 기준이므로 예열 중 진행된 시뮬레이션 시간까지 완전히 동일하다는 뜻은 아니다.

| 일반 수 | 그림자 | 이동·충돌 평균 ms/고정 갱신 | 표시 갱신 평균 ms/프레임 | 전체 프레임 간격 평균 ms | 활성 Renderer | 그림자 Renderer |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 300 | 제거 전 | 123.90 | 3.92 | 2,061.92 | 615 | 301 |
| 300 | 제거 후 | 124.18 | 3.42 | 2,123.57 | 314 | 0 |
| 500 | 제거 전 | 218.61 | 6.00 | 3,688.35 | 1,015 | 501 |
| 500 | 제거 후 | 229.06 | 5.30 | 3,830.35 | 514 | 0 |

표시 갱신 CPU 시간은 약 12.7% / 11.7% 감소했다. 이 값은 Presenter와 View 속성 갱신이며 GPU 시간·전체 렌더 시간이 아니다. Renderer 수를 draw call 수로 해석하지 않는다. 그림자 제거로 전체 FPS가 개선됐다는 증거는 없다. 이동·충돌 계산은 거의 동일한 수준이고 500 조건에서는 오히려 약 4.8% 높게 측정됐다. 모델 코드는 변경하지 않았으며 짧은 단일 비교의 변동을 그림자 제거에 의한 모델 악화라고 단정하지 않는다.

두 조건 모두 한 프레임에 고정 갱신이 최대 17회 실행됐다. 이동 계산 한 번부터 20ms 주기를 초과해, 누적 갱신이 한 프레임에 몰리는 현상을 실제 Unity에서 확인했다. 위 수초 단위 프레임은 적을 죽이지 않고 모으는 이 진단의 관측이며 정상 생존 플레이의 FPS가 아니다. 그림자만 제거해서 500마리를 원활하게 지원했다고 결론내리지 않는다.

이 Unity 런타임의 `GC.GetAllocatedBytesForCurrentThread()`는 의도적인 1KB 할당 탐침에서도 증가하지 않았다. 보고서의 `allocationCounterSupported=false`, 바이트 값 -1은 미지원이며 할당 0을 뜻하지 않는다. 이전 순수 .NET 모델의 할당 측정과 구분한다.

## 증거와 검증

- 공식 MCP stdio에서 초기화·도구 151개·프로젝트/버전 조회 성공: `Logs/Validation/unity-plugin-migration-1789645736476/mcp-shadow-comparison-20260917.json`. 연결 종료 확인도 `connect-unity.ps1`에서 ready/stopped로 통과했다.
- 비교 제거 전: 검증 `b7adc7c5a2f04ea5b23b6284b373563e`, 소스 `53a5fd43eae908df4a8b83c1b788a2c8bd9d359042211bb8849854e661dc64de`, 2/2 통과. 보고서 ID 300=`93ac1f180d244ac9b26e68cfa36e18f1`, 500=`ef453cbbba6040588e550790cc02d3ea`.
- 비교 제거 후: 검증 `87b8b74ea1434f5aaeda426281d8e9ff`, 보고서 ID 300=`2f5cce2ab21541e2affca43a2b6926e4`, 500=`393584ba990745dfb75facc035278402`, 2/2 통과.
- 현재 소스 `310c328ec8f2118f392c1bcf8f6bdb9607b19f75e12b5a2c3cce762a05cff637`: 비교 2개, FeedbackAndPool 3개(`b9695fe4a7d84954bfa93f2f02bd8175`), MonsterPresentation 2개(`60d8e215d78c438fa4e8bbeff7c26acf`), MonsterStability 8개(`4a5f16263e974069a2dba5cefa21c74f`) 통과. manifest·receipt·XML·현재 소스 해시 대조, 오류 0.
- 관련 원시 파일과 요약: `Logs/Validation/shadow-cap-comparison-20260917/`의 `before.json`, `after.json`, `verified-results.json`, `asset-apply.json`, `final-readback.json`, 사용자 설정 해시 전후 기록. 개별 측정은 `Logs/Validation/CrowdPerformance/<보고서 ID>/report.json`이다.
- 최초 15초 측정 `1b839d91813e44238b57d4287d612c1e`는 7/4 프레임으로 표본 부족 실패다. 다음 `1d2b54adacf7479f90e0033d9bc82261`은 외부 소스 수정이 Editor에 아직 재컴파일되지 않아 이전 15초 코드로 실행되어 8/5 프레임으로 실패했다. 둘 다 성공 비교에서 제외하고 공식 recompile 완료 후 새 결과로 검증했다. 최종 fixture는 표본 20개를 요구하고 측정 90초를 넘으면 실패한다.
- 새 장시간 검사·전체 검사·Windows 빌드는 실행하지 않았다. 기존 exe는 이번 변경 전이다.

## 후속 대화: 몸통 80%와 피격 영역 110% 제안

사용자는 충돌로 서로 밀려나는 판정을 제거하고 몸통은 스프라이트의 80%, 공격에 맞는 몬스터 피격 영역은 110%로 분리하는 구조를 제안했다. 110%는 무기 이펙트 확대가 아니라 몬스터 피격 영역이라는 답변을 확인했다. 구조 평가 요청 단계이며 이 군중·판정 변경은 아직 구현하지 않았다.

현재는 Unity Rigidbody2D 물리가 아니라 자체 원형 판정이며 `BodyRadius`를 이동, 접촉 피해, 검·창·도끼 타격, 파이어볼 명중·폭발에 함께 사용한다. 크기만 줄이면 공격도 잘 안 맞게 바뀌므로 이동 반경과 피격 반경을 분리해야 한다. 무기 범위·궤적은 유지하되 더 큰 피격 영역은 가장자리에서 맞는 시점을 바꾸므로 기존 적중 결과가 완전히 동일하다고 설명하지 않는다. 검색 후보 반경도 피격 반경에 맞춰 넓혀 가장자리 대상을 누락하지 않아야 한다.

권장안은 지형 충돌·접촉 피해용 몸통 80%, 무기 피격용 영역 110%, 몬스터 상호 물리 밀어내기 해제다. 반복 겹침 보정과 복잡한 우회 접선 계산을 줄이고, 완전한 한 점 겹침이 거슬리면 근접 이웃을 한 번 조회하는 약한 이동 방향 회피를 둔다. 작은 비트리거 Collider끼리 물리 충돌을 켜면 기존과 같은 밀어내기 비용이 새 경로에서 발생하므로 자동으로 가벼워진다고 볼 수 없다. 먼저 기존 공간 색인과 원형 거리 판정을 유지하는 편이 수정 범위가 작다.

원본 스프라이트의 투명 여백을 제외한 몸통 기준으로 비율을 정하고 발 위치·피격 팽창에 판정 크기가 흔들리지 않게 해야 한다. 몬스터끼리 충돌할 때의 밀어내기와 공격 피격 넉백은 별개이며, 피격 넉백은 유지하는 안이다. 장애물 통과 금지와 공중의 지상·장애물 무시는 유지한다. 다음 구현 시 이 규칙과 기존 군중·타격·공간 조회 테스트를 함께 바꾸고 동일 300/500 조건을 비교한다.

---

# 성능 조사 및 조정안 — 2026-09-17

조사 대상: 파이어볼 수명과 Unity 플레이 중 성능 저하. 기준 게임 코드: `8ef1ca9`, 조사 시작 HEAD: `6deab1e`.

## 결론

우선 개선 대상은 **군중 겹침 보정과 그 안에서 반복되는 지형 검사·임시 메모리 할당**이다. 일반 몬스터 상한 300을 적용한 상태에서도 밀집 시 비용이 커질 수 있는 구조를 확인했다. 300~500마리는 최적화 후 검증할 대상 규모로 삼고, 현재 일반 상한을 임의로 늘리거나 더 낮추지는 않는다.

파이어볼은 코드·연결된 설정에 3초 수명이 있고, 별도 모델 실행에서도 3초에 제거됐다. 따라서 현재 증거로는 무제한 생존 버그가 재현되지 않았다. 화면 이탈 시 회수하는 규칙은 없으며, 속도 8 × 수명 3초로 발사 지점에서 최대 24유닛을 이동한다. 사용자가 본 현상의 런타임 원인은 Editor가 열린 상태에서 투사체 ID·생성 시각·종료 시각·표시 객체를 함께 확인해야 한다.

이번 작업은 원인 조사와 조정안 작성이다. 실제 게임 코드는 수정하지 않았다. 아래 수치는 Unity FPS나 수정 후 성능 수치가 아니다.

## 짧은 모델 측정

Unity Editor는 실행 중이지 않았다. 저장소 Core C# 125개를 Git 제외 경로에 복사하고 시간·할당·호출 횟수 계측만 추가해 .NET `10.0.11`, Release로 실행했다. 실제 `RunTestRig`와 이동 코드를 사용했으며, 빈 지형에서 플레이어는 정지하고 지상 적이 추적하도록 했다. 플레이어 AI·전투·스폰·Unity 표시·Editor 비용은 제외했다.

각 조건은 40회 준비 후 60회 측정, 고정 주기 0.02초다. 밀집 조건의 초기 배치 간격은 0.58, 일반 적 지름은 0.52다. 500마리는 현재 게임의 스폰 상한을 변경한 것이 아니라 진단용 직접 생성이다. 임시 할당은 해당 스레드의 누적 할당 차이이며, 남아 있는 메모리 크기나 GC 정지 시간과 다르다.

| 조건 | 이동 전체 평균 | 이동 전체 p95 | 군중 겹침 보정 평균 | 임시 할당/고정 주기 |
| --- | ---: | ---: | ---: | ---: |
| 100마리 밀집 | 4.41ms | 5.70ms | 3.55ms | 6.90MB |
| 300마리 밀집 | 13.55ms | 16.96ms | 10.86ms | 20.50MB |
| 500마리 밀집 | 21.05ms | 28.38ms | 16.41ms | 31.21MB |
| 300마리 분산, 간격 1.5 | 4.54ms | 5.99ms | 0.24ms | 4.63MB |

MB는 1,000,000바이트 기준이다. 첫 계측에서도 300마리 12.82ms 중 보정 10.39ms, 500마리 19.54ms 중 보정 15.18ms로 같은 구간이 크게 나타났다. 할당 위치를 추가 계측한 표의 실행에서는 300마리 임시 할당 약 20.50MB 중 **17.47MB, 약 85%가 군중 겹침 보정 구간**에서 발생했다. 해당 조건에서 원형 공간 조회는 주기당 약 2,106회, 지형 후보 셀 열거는 약 12,284회였다.

이는 합성 조건의 짧은 원인 분리 측정이다. 실제 맵, 무기 성장, GPU, Editor와 Mono/IL2CPP의 비용은 별도이며 이 수치로 실제 FPS를 환산하거나 500마리 안정 동작을 선언하지 않는다. 분산 조건은 밀도와 위치가 함께 달라져 경로 탐색 상황도 같지 않다.

## 원인별 판단

### 1. 군중 보정의 반복 지형 검사와 할당 — 우선순위 가장 높음

- `Core/Movement/CrowdSolver.cs:73`은 최대 6회 보정하며 각 지상 유닛의 주변을 다시 조회한다. 실제 충돌 쌍은 ID 비교로 한 반복 안의 중복 처리를 피하고 있지만, 주변 후보 조회는 유닛별로 반복한다.
- 겹친 두 몸체를 움직일 때마다 `CircleSweep.MoveAndSlide`를 각각 호출한다(`CrowdSolver.cs:86`). 작은 밀집 보정에도 장애물 검사 경로를 통과한다.
- `MoveAndSlide`는 시작·종료 위치를 확인하고 `SweepCircle`도 시작 위치를 확인한다. 이 안전 검사의 의미는 유지하면서 중복 계산과 할당을 줄일 필요가 있다.
- `Core/Movement/CircleSweep.cs:87`은 후보 셀 열거마다 새 `HashSet<GridCell>`을 만든다. 이동량이 0인 정적 원형 검사도 같은 열거 경로를 사용한다.
- `Core/World/SpatialIndex.cs:69`은 원형 조회마다 새 List를 만들고 정렬한 뒤 새 읽기 전용 래퍼를 반환한다. 이동·군중·전투에서 모두 사용하는 경로다.

현재 구조에서 군중 보정이 가장 비싼 구간이라는 것은 위 모델 계측으로 확인했다. 개별 할당 종류가 전체 바이트 중 정확히 얼마인지는 아직 할당 스택으로 나누지 않았다. 따라서 모든 할당을 HashSet 하나의 탓으로 단정하지 않는다.

### 2. 화면 밖 데이터를 매 프레임 훑는 표시 경로 — 장시간 누적 위험

`Presentation/WorldPresenter.cs:25,42`는 저장된 지형과 경험치 전체를 순회한 후 표시 거리를 검사한다. 경험치 공간 색인은 이미 존재하지만 화면 표시에서는 활용하지 않는다. 몬스터 수를 제한해도 경험치·방문 지형의 누적 비용은 남는다.

`InitialRunBuilder.PrepareTerrain`은 주변 청크를 준비하고 `WorldStore`는 기존 지형을 보관한다. 현재 일반 플레이 조립 경로에는 지형 캐시 퇴거 호출이 없다. 보존해야 할 경험치·몬스터의 논리 데이터와 재생성 가능한 지형 캐시를 구분할 필요가 있다. 캐시 제거 시 현재 `TerrainRevision`이 전체 길찾기 캐시를 무효화하므로 단순 퇴거만 추가하면 재탐색 급증을 만들 수 있다.

### 3. 표시 객체와 공격 메시 갱신 — Unity에서 추가 측정 필요

`Unity/Views/UnitView.cs`는 유닛별 몸체, 그림자 메시, 체력바 앞·뒤 Renderer를 유지하고 매 표시 프레임 여러 속성을 다시 설정한다. 500마리가 표시되면 몸체·그림자 1,000개에 체력바가 최대 1,000개 추가될 수 있다. Renderer 수와 실제 draw call 수는 다르므로 Frame Debugger로 확인해야 한다.

`AttackView.Show`의 검·창·도끼 범위와 `ShowExplosion`은 `ShapeRenderer` 메시를 재작성한다. `ShapeRenderer.cs:69`은 정점·색·UV·삼각형 업로드와 bounds 재계산을 한다. 공격 개수·연속 횟수가 커질수록 이 비용도 늘어난다. 경험치 메시와 몬스터 그림자는 매 프레임 재생성하는 구조가 아니므로 같은 문제로 묶지 않는다.

표시 풀은 이미 구현돼 있다. 풀을 새로 넣는 것만으로 해결된다고 보지 않는다. 풀 안의 고유 메시·오브젝트 수와 매 프레임 갱신량을 줄이는 방향을 검토한다.

### 4. 길찾기 및 고정 주기 비용 — 후속 순위

근처 적은 반폭 12셀의 FlowField를 공유하지만, 범위 밖에서는 적별 PathRequest를 사용한다. `GroundChaseBehavior.Tick`은 매 고정 주기에 `ChaseDirection`을 호출하며 먼 적의 판단 주기를 낮추는 별도 스케줄러는 없다. 탐색 노드에는 512개 예산이 있지만 요청 생성 시 시작·목표 Anchor 검사는 그 예산 밖에서 동기로 수행된다.

이번 빈 지형 측정에서는 `Navigation.Advance` 자체가 최대 병목은 아니었다. 장애물이 많거나 플레이어가 계속 이동할 때 추가로 확인할 항목이다.

게임은 초당 50회 고정 갱신을 수행한다. 프레임이 늦어지면 Unity가 한 프레임에 여러 고정 갱신을 처리할 수 있어 비싼 이동 계산을 증폭시킬 가능성이 있다. 실제 이번 플레이에서 이 현상이 발생했는지는 아직 측정하지 않았다. [Unity 고정 갱신 설명](https://docs.unity3d.com/6000.0/Documentation/Manual/fixed-updates.html)

## 파이어볼 조정안

현재 실행 확인: 2.98초에는 1개, 3초에는 0개, 이동 거리 24, 수명 만료 폭발 0회. 설정 파일은 실제 `GameCatalog.asset`의 GUID가 참조하는 `DevelopmentDefaults.asset`이며 speed 8/lifetime 3이다.

권장 변경은 **기존 수명에 화면 출구 회수 조건을 추가**하는 것이다.

- 명중: 첫 적 위치에서 폭발하고 투사체를 회수한다.
- 비명중: 3초 수명 또는 진행 방향의 화면 출구 밖 여유 거리 도달 시 먼저 회수한다. 여유 거리는 설정값으로 둔다.
- 단순 카메라 이동이나 화면 밖에서 생긴 무기 복사본 때문에 즉시 삭제되지 않도록 진입·출구를 구분한다. 끝나는 주기에도 종료 경계 이전의 명중 판정은 보존한다.
- 회수는 논리 투사체·표시 풀·공격 판정 캐시까지 한 경로로 정리하며, 자연 회수는 폭발 피해나 경험치를 만들지 않는다.
- 화면 밖 타격 거리가 줄어드는 게임 규칙 변경이다. 기존 `OffscreenProjectilesRemainUntilTheirFlightLifetimeAndDoNotExplodeOnExpiry` 테스트와 설계 문서를 함께 갱신해야 한다. 폭발 반경 강화는 계속 폭발 반경에만 적용한다.

이 변경으로 현재 확인되지 않은 무한 생존 버그를 고쳤다고 보고하지 않는다. Editor 재현에서 3초를 넘긴 논리 객체나 남은 View가 발견되면 별도 수명 버그로 추적한다.

## 권장 구현 순서

1. **규칙을 유지하는 할당 제거.** 정지 원형 지형 검사는 중복이 없는 좌표 루프로 처리하고, 이동 sweep은 호출별 재사용 버퍼나 할당 없는 열거를 사용한다. QueryCircle에는 호출자가 소유한 결과 버퍼를 추가한다. 중첩 호출에서 버퍼를 공유해 결과를 덮어쓰지 않으며 현재 ID 순서·경계 포함·큰 좌표·벽 통과 방지 계약을 유지한다.
2. **군중 처리 재사용.** 유닛 목록·후보쌍·보정 작업 공간을 재사용하고 동일 반복 내 중복 조회·지형 검사를 줄인다. 먼저 현재 6회 보정 결과를 유지한 채 비용 감소를 확인한다. 그 후 필요하면 밀도·잔여 겹침 기준으로 보정 횟수를 조절하는 안을 별도 검증한다. 단순히 횟수만 1~2회로 줄여 겹침을 숨기지 않는다.
3. **파이어볼 종료와 관측.** 위 출구 규칙을 적용하고 생성·명중·수명 만료·화면 퇴장·현재 활성 수를 구분해 기록한다. 실제 플레이에서 수명과 View 반환을 확인한다.
4. **표시 범위 조회와 렌더링 경량화.** 경험치·지형 표시 후보를 인접 청크에서 구하고, 보간 중 화면에 들어오는 객체도 빠지지 않게 여유를 둔다. 정적 그림자·경험치·공격 형상의 메시/스프라이트를 공유하고 변경된 색·스케일·체력만 갱신한다. 공격 판정과 표시 반경의 일치를 유지한다.
5. **필요한 경우 길찾기 구조 확장.** 먼 적의 판단·재탐색을 분산하고 가까운 적은 공유 경로를 우선한다. 개체·경험치는 보존하며 이동·피격·접촉을 임의로 생략하지 않는다. 현재 결과만으로 DOTS/ECS 전체 전환은 선택하지 않는다.

무한 강화는 개수 × 연속 횟수 × 초당 발동 수가 함께 증가하는 구조다. 투사체 수명이 있어도 동시 처리량에는 별도의 상한이 없다. 논리 타격을 삭제하거나 피해 배율 하나로 합치는 방식은 기존 무한 성장·궤적·명중 시점 계약과 충돌하므로 이번 1차 최적화에 포함하지 않는다.

## 검증 계획

- 우선 CircleSweep·CrowdMovement·Fireball 및 관련 공간 조회의 범위 검사만 선택 실행한다. 장애물 모서리, 밀집 통로, 넉백, 공중 통과, 종료 경계 명중, 이전 View 재사용을 확인한다.
- 같은 시드·배치·몬스터 100/300/500 조건에서 전후 모델 평균/p95/할당을 비교한다. 합성 모델 결과와 Unity 결과는 별도로 저장한다.
- Unity에서는 30~60초의 짧은 300/500마리 밀집·분산 구간, 파이어볼 성장 구간, 바닥 경험치 누적 구간을 각각 측정한다. 모델·View·렌더·GC·프레임당 FixedUpdate 횟수를 분리한다.
- 60FPS를 선택할 경우 전체 프레임 16.7ms가 목표 예산이다. 평균만 보지 않고 p95 및 긴 프레임을 확인한다. 현재 자료로 달성을 보장하지 않는다.
- Editor와 게임 자체 비용은 구분한다. 같은 소스로 만든 실행 파일 검증은 별도 빌드 범위에 포함될 때 실시한다. 이번에는 장시간 검사·Unity 전체 재검사·새 게임 빌드를 실행하지 않았다. [Unity의 플레이 모드/Editor/실행 플랫폼별 측정 안내](https://docs.unity3d.com/6000.0/Documentation/Manual/profiler-profiling-applications.html)

## 증거와 재현

`Logs/Validation/performance-investigation-20260917/`:

- `result.json`: 첫 이동 비용·파이어볼 수명 확인.
- `allocation-breakdown.json`: 할당 위치 및 후보 셀 호출 횟수 추가 계측.
- `source-manifest.json`: 계측에 사용한 원본 Core 125개 파일의 SHA256.
- `prepare.ps1`, `Probe.csproj`, `Program.cs`, `NuGet.Config`: 임시 진단 재현용. 외부 패키지 의존성 없음.
- `Source/`: 계측용 복사본. 게임 소스를 교체하는 파일이 아니며 Unity Assets에 넣지 않는다.

---

# Unity MCP 개선 확인 보고

확인 시각: 2026-09-17 22:44 KST. 대상: `C:/Users/ace21/Desktop/SsalMuk`, Unity `6000.4.6f1`.

## 결론

공식 Unity 플러그인과 CLI로 전환되어 별도 Python 서버 및 전용 연결 브리지를 관리할 필요가 줄었다. 공식 스킬 31개도 설치되어 Unity 기능별 작업 지침을 사용할 수 있다. 교체 당시 실제 Editor 호출은 성공했다.

현재는 실행 중인 Unity Editor가 없어 실시간 제어 연결을 재확인하지 못했다. 공식 MCP 프로세스의 초기화는 성공하지만 제공 도구는 0개이고, CLI 상태는 `STATUS_NO_INSTANCES`다. 설치 완료와 현재 Editor 연결 상태를 구분해야 한다.

## 연결 구조의 변화

이전:

```text
Codex unityMCP
  → 별도 uv/Python mcpforunityserver 10.2.0 (127.0.0.1:8080)
  → com.coplaydev.unity-mcp / 프로젝트 전용 연결 브리지
  → Unity Editor
```

현재:

```text
공식 Unity 스킬 → 작업 지침 제공
Codex → 공식 Unity CLI의 stdio MCP → 프로젝트의 Pipeline → Unity Editor
터미널 → 공식 Unity CLI command     → 같은 Pipeline      → Unity Editor
```

stdio는 Codex와 CLI 사이의 통신 방식이다. Pipeline은 Editor 내부의 로컬 서버를 계속 사용한다. 모든 로컬 서버가 사라진 구조는 아니다. Pipeline은 교체 이전부터 프로젝트에 있었으며 이번에 새로 추가한 패키지가 아니다.

| 구성 | 확인 결과 |
| --- | --- |
| Codex 플러그인 | `unity@unity-agent-plugin` `0.1.6-beta`, 설치·활성화, 스킬 31개 |
| Unity CLI | `1.0.0-beta.10` |
| Pipeline | manifest·lock에 `com.unity.pipeline` `0.7.0-exp.1` |
| Codex MCP | `unity` 활성화, CLI 절대 경로와 SsalMuk 프로젝트 경로 지정 |
| 기존 구성 | `unityMCP` 설정, Coplay 패키지, `UnityMcpConnection.cs` 제거 확인; 8080 리스너 없음 |

공식 플러그인은 Codex에 설치하는 작업 지침 묶음이다. Unity Package Manager로 설치하는 게임 패키지와는 구분된다. [공식 설명](https://docs.unity.com/en-us/ai/unity-plugin/about-unity-plugin)

## 좋아진 점과 적용 범위

| 항목 | 구체적인 변화 | 이 프로젝트에서의 의미 |
| --- | --- | --- |
| 연결 운영 | 별도 Python HTTP 서버와 전용 연결 브리지 제거, 공식 CLI 진입점 사용 | 서버 실행 순서와 프로젝트 전용 연결 코드의 유지 관리 부담 감소 |
| 작업 지침 | Unity 기능 담당팀이 작성한 공식 스킬 31개 추가 | 픽셀 렌더링, 스프라이트 피벗·슬라이싱, uGUI, URP 발광 표현 작업에 해당 기능의 절차를 참고 가능 |
| 상태 진단 | `editor_status`, `recompile_status`, `eval` 등으로 상태와 실제 값을 읽는 경로 정리 | 재생 중, 컴파일 중, 대화상자 대기, 프로젝트 불일치를 구분하는 근거 확보 |
| 연결 확인 | `connect-unity.ps1`이 실제 프로젝트 경로와 Unity 버전을 읽고 예상값과 대조 | 도구 목록이나 서버 응답만으로 연결 성공을 판단하지 않음 |

31개 스킬의 범위와 Unity 6 이상 지원은 [Unity 공식 발표](https://unity.com/blog/unity-plugin-codex)에서도 확인했다. 스킬은 개발 절차이며 게임에 31개 기능이 자동 추가된다는 뜻은 아니다.

씬·에셋 조작과 테스트 실행은 기존 MCP에서도 제공하던 범주다. 해당 기능 전체가 이번 교체로 처음 가능해졌다고 보지는 않는다. 이번 변경의 확인 가능한 이점은 연결 구성 정리, 공식 작업 지침 추가, 진단 경로의 명확화다.

## 확인 근거

이번 확인:

- 설치 목록에서 공식 플러그인의 버전·활성 상태 확인, 로컬 스킬 폴더 31개 확인.
- Codex `unity` MCP의 stdio 설정 및 프로젝트 경로 확인.
- manifest·lock, 이전 커밋의 시작 스크립트, 현재 연결 스크립트 비교.
- 공식 MCP의 `initialize`와 `tools/list` 응답 확인. 현재 도구 수 0개.
- CLI가 Editor 인스턴스 0개를 반환했고, 실제 Unity Editor 실행 파일의 프로세스도 없음. 실행 중인 동명 `unity.exe`는 CLI이므로 Editor로 세지 않음.
- 기존 8080 포트의 리스너 없음.

과거 성공 기록을 이번에 다시 읽어 확인한 내용:

- 2026-09-17 21:04:53 KST: 공식 MCP 도구 151개 조회, `editor_status` 및 `eval` 성공. 실제 SsalMuk 경로와 Unity `6000.4.6f1` 일치.
- 21:06:28 KST: 프로젝트 시작 래퍼를 통한 같은 MCP 호출 성공.
- 교체 완료 시점: 컴파일 `up_to_date`, 오류 없음. 실제 등록 패키지 조회에서 Coplay 없음·Pipeline `0.7.0-exp.1` 확인.

위 151개 및 컴파일 성공은 당시 실행 결과다. 현재 열린 Editor에서 다시 검증한 결과로 취급하지 않는다. 현재 대화에 직접 노출된 Unity MCP 도구가 없어 이번 검사는 CLI와 별도 stdio 프로브로 수행했다.

## 한계 및 다음 연결 확인

- 실행 속도, 토큰 사용량, 장시간 안정성의 전후 비교 측정은 하지 않았다. 더 빠르거나 오류가 몇 퍼센트 줄었다는 수치는 제시할 수 없다.
- 플러그인·CLI는 beta, Pipeline은 experimental 버전이다. 교체 중 패키지 제거에 따른 재로딩 과정에서 상태 조회 시간 초과가 한 번 있었고, 이후 재접속 및 패키지·컴파일 상태 확인은 성공했다.
- 몬스터 이동 보간·길찾기·개체 상한 변경은 게임 코드 커밋 `8ef1ca9`의 작업이다. MCP 교체의 FPS 개선 효과로 산정하지 않는다.
- 기존 파일 기반 검증 하네스와 Unity Test Framework는 유지한다. 이번 보고를 위해 게임 테스트, 장시간 검사, 빌드를 새로 실행하지 않았다.
- SsalMuk Editor를 다시 연 뒤 `tools/validation/connect-unity.ps1`로 프로젝트·버전·실제 읽기를 확인해야 현재 연결을 완료 상태로 판단할 수 있다.

## 증거 위치

- 도구 교체 커밋: `56eff16`.
- 현재 상태: `Logs/Validation/mcp-report-20260917/current-state.json`.
- 현재 MCP 도구 목록: `Logs/Validation/mcp-report-20260917/mcp-tools.json`.
- 과거 성공 기록: `Logs/Validation/unity-plugin-migration-1789645736476/mcp-after.json`, `mcp-wrapper.json`, `recompile-final.json`, `loaded-packages-final.json`.
- 연결 절차: `docs/development/HARNESS.md` 3절, `tools/validation/connect-unity.ps1`.

원시 증거는 Git에서 제외되는 `Logs/Validation/`에 보관한다. 이번 요청에서는 보고서만 작성하고 게임 코드·도구 설정을 변경하지 않았다.
